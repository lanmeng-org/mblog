environment: "development"  # development production

db:
  type: "mysql"  # 支持mysql
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "mm9113"
  database: "mblog"

web:
  listen: ":80"
  template:
    file_suffix: ".html"
    paths: ["template/*", "template/public/*", "template/widget/*"]